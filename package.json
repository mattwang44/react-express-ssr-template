{
  "name": "react-express-ssr",
  "version": "1.0.0",
  "description": "",
  "main": "src/server/server.js",
  "scripts": {
    "dev": "nodemon",
    "start": "node src/server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-dev-client": "cd client; yarn install && yarn build; cp -r ./build ../src/client"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.17.1",
    "express-validator": "^6.13.0",
    "helmet": "^3.21.2",
    "mongoose": "^5.8.4",
    "nodemon": "^2.0.15"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "prettier": "^1.19.1"
  },
  "nodemonConfig": {
    "watch": [
      "src/server/"
    ],
    "delay": "1000",
    "ext": "js,"
  }
}
